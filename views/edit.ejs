<% include('layout', { body: null }) %>
<h2><%= producto ? 'Editar Producto' : 'Agregar Producto' %></h2>
<form action="<%= producto ? '/edit/' + producto.id : '/add' %>" method="POST">
  <label>Nombre: <input type="text" name="nombre" required value="<%= producto ? producto.nombre : '' %>"></label>
  <label>Precio: <input type="number" name="precio" step="0.01" required value="<%= producto ? producto.precio : '' %>"></label>
  <label>Stock: <input type="number" name="stock" required value="<%= producto ? producto.stock : '' %>"></label>
  <button type="submit"><%= producto ? 'Actualizar' : 'Guardar' %></button>
</form>
